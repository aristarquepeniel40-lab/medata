% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00b_import_xlsx.R
\name{import_xlsx}
\alias{import_xlsx}
\title{Importer un dataset depuis un fichier Excel}
\usage{
import_xlsx(path, name, metadata, sheet = 1, schema = NULL, ...)
}
\arguments{
\item{path}{Chemin du fichier \code{.xlsx}/\code{.xls}.}

\item{name}{Nom du dataset (voir \code{mecore::me_dataset}).}

\item{metadata}{Un \code{mecore::me_metadata}.}

\item{sheet}{Nom ou index de la feuille a lire (par defaut : la
premiere).}

\item{schema}{Un \code{mecore::me_schema} optionnel. Si fourni et non
respecte, leve une erreur (le dataset n'est jamais retourne invalide).}

\item{...}{Arguments additionnels passes a \code{readxl::read_excel()}.}
}
\value{
Un \code{mecore::me_dataset}.
}
\description{
Lit une feuille Excel et construit un \code{mecore::me_dataset}, sur le
meme modele qu'\code{import_csv()}. Si \code{schema} est fourni, valide les
donnees AVANT de retourner l'objet (via \code{mecore::check_schema()}).
}
\details{
Necessite le package \code{readxl} (\code{Suggests}, pas \code{Imports} — coherent
avec le traitement d'\code{openxlsx} dans \code{mecore::export_logframe_xlsx()}).
}
