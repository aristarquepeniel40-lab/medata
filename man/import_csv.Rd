% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_import.R
\name{import_csv}
\alias{import_csv}
\title{Importer un dataset depuis un fichier CSV}
\usage{
import_csv(path, name, metadata, schema = NULL, ...)
}
\arguments{
\item{path}{Chemin du fichier CSV.}

\item{name}{Nom du dataset (voir \code{mecore::me_dataset}).}

\item{metadata}{Un \code{mecore::me_metadata}.}

\item{schema}{Un \code{mecore::me_schema} optionnel. Si fourni et non
respecte, leve une erreur (le dataset n'est jamais retourne invalide).}

\item{...}{Arguments additionnels passes a \code{utils::read.csv()}
(ex. \code{sep = ";"}, \code{dec = ","}).}
}
\value{
Un \code{mecore::me_dataset}.
}
\description{
Lit un CSV et construit un \code{mecore::me_dataset}. Si \code{schema} est
fourni, valide les donnees AVANT de retourner l'objet (via
\code{mecore::check_schema()}) — comble l'angle mort identifie : jusqu'ici,
rien n'empechait \code{meindicator::compute_indicator()} de calculer un
indicateur sur des donnees mal typees.
}
